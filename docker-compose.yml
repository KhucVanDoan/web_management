version: '3.7'

services:
  mesx-web:
    image: registry.vti.com.vn/snp_smartwms/source/web:develop
    ports:
      - '10088:80'
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
